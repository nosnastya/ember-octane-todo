export default function( /* server*/ ) {

    // server.createList('list', 10);

    // generate 10 lists
    // server.loadFixtures('todos');
    // server.loadFixtures();

    // server.createList('list', 10, 'withTodos');

}